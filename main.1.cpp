#include <iostream>
#include <math.h>

using namespace std;

 int main()

 {

 const double E=2.718
 double x, y, z, r;
 x= -2.235 * row(10;-1);
 y= 2.25;
 z= 15.221;
 r= (row(E;fabs(x-y))*row(fabs(x-y);x+y)/atan(x)+atan(y))+pow(row(x;5)+row(log10(y);e));
  cout<<r<<endl;


 return 0;
 }
