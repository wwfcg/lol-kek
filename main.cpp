#include <iostream>
#include <math.h>

using namespace std;

 int main()

 {
 double x, y, z, r; fofqjfoqfj
  
 x = -2.235e-1;
 y = 2.25;
 z = 15.221;
 r = (pow(M_E,fabs(x-y))*pow(fabs(x-y),x+y))/(atan(x)+atan(z))+ pow(pow(x,5)+pow(log10(y),3),1/3);
  cout<<r<<endl;

 return 0;
 }
